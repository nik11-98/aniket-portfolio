/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the Next.js dev indicator badge (bottom-left "N").
  devIndicators: false,
};

export default nextConfig;
